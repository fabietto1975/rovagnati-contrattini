<?php

namespace backend\app\dao;

use backend\common\dao\DAOAbstract;
use backend\common\dao\DAOInterface;

class ProdottoRovagnatiDAO extends DAOAbstract implements DAOInterface {

    public function __construct() {
        parent::__construct();
    } 

    public function create($item) {
        //NOP
    }

    public function delete($id) {
        //NOP
    }

    public function readProdottoRovagnatiPerLivello($id_livello) {
        
        switch ($id_livello) {
            case "1":
                $sql = "select distinct (prodotto) as ITEM from anagrafica_prodotti_rovagnati order by prodotto;";
                break;
            case "2":
                $sql = "select distinct (tipo) as ITEM from anagrafica_prodotti_rovagnati order by tipo;";
                break;
            case "3":
                $sql = "select distinct (linea) as ITEM from anagrafica_prodotti_rovagnati order by linea;";
                break;
            case "4":
                $sql = "select distinct (famiglia) as ITEM from anagrafica_prodotti_rovagnati order by famiglia;";
                break;
            case "5":
                $sql = "select distinct (divisione) as ITEM from anagrafica_prodotti_rovagnati order by divisione;";
                break;
            case "6":
                $sql = "select distinct (descrizione) as ITEM from anagrafica_prodotti_rovagnati order by descrizione;";
                break;

            default:
                break;
        }
        $stmt = $this->getPdo()->prepare($sql);
        $stmt->execute();
        return $stmt->fetchAll();
        
    }

    public function update($item) {
         //NOP
    }

    public function readByID($id) {
        
    }

    public function read() {
        $stmt = $this->getPdo()->prepare("select * from anagrafica_prodotti_rovagnati order by codice_articolo;");
        $stmt->execute();
        return $stmt->fetchAll();
    }

}
